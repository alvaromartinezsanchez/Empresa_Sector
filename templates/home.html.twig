{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}


{% if app.user.role is defined %}
    <div class="container mt-5">
        <h4> Bienvenido {{ app.user.nombre }}, visita nuestras paginas: </h4>

        <div class="row justify-content-around mt-5">
            <a class="btn btn-success col-3" href="{{ path('empresa_index') }}">Empresas</a>
            <a class="btn btn-success col-3" href="{{ path('sector_index') }}">Sectores</a>
            {% if app.user.role == 'ROLE_ADMIN' %}
                <a class="btn btn-success col-3" href="{{ path('usuario_index') }}">Usuarios</a>
            {% endif %}
        </div>
    </div>
{% else %}
    <div class="container mt-5">
        <h4>Debes estar logeado para acceder al sistema, puedes iniciar/cerrar sesion haciendo click en los enlaces login/logout de la barra de navegacion.</h4>
        <h5>Si es la primera vez que accedes puedes utilizar los Usuarios por defecto: </h5>
        <div class="row mt-3 ml-5">
            <ul>
                <li><h6>Email: admin@admin.com Password: admin</h6></li>
                <li><h6>Email: cliente@cliente.com Password: cliente</h6></li>
            </ul>
        </div>
    </div>
{% endif %}

{% endblock %}

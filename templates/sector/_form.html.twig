{{ form_start(form) }}
<div class="row justify-content-center">
    <div class="col-8">
        <div class="row justify-content-center py-3"  style="background-color: gray; border-radius: 10px; border: 1px solid black">
            <div class="col-2 align-self-center">
            <strong>
                {{ form_label(form.nombre) }}
                </strong>
            </div>
            <div class="col-8">
                {{ form_widget(form.nombre) }}
            </div>
            
                <div class="col-10 col-md-2">
                    <button class="btn btn-success col-12">{{ button_label|default('Save') }}</button>
                </div>
            
        </div>
    </div>
</div>
{{ form_end(form) }}
